# Local preview icons

Generated bank-offer logos stay local and are excluded from Git. Run
`node scripts/collect_partner_offer_icons.mjs` and
`node scripts/build_partner_offers_preview.mjs` from `app/` to prepare them.
The public demo uses the built-in store icon.
